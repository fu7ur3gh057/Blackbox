# Backend URL the dev server proxies to. Override if your backend lives
# elsewhere (e.g. tunnel from prod). Production builds ignore this — the
# Next bundle is served by FastAPI at the same origin as /blackbox/api.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8765
