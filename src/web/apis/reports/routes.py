"""Reports endpoints — пока пустой роутер. Подключим, когда решим что
именно отдавать наружу (digest на лету, recent_errors, метрики чеков)."""

from fastapi import APIRouter

router: APIRouter = APIRouter()
