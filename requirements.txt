httpx[socks]>=0.27
pyyaml>=6.0
psutil>=5.9
asyncpg>=0.29
rich>=13.7
questionary>=2.0
fastapi>=0.115
uvicorn[standard]>=0.32
taskiq>=0.11
sqlmodel>=0.0.22
aiosqlite>=0.20
bcrypt>=4.0
PyJWT>=2.8

# Web terminal — PAM auth against system users. Pip-installable wrapper
# around libpam.so.0 (libpam0g on debian/ubuntu, pam on rhel/fedora — the
# host lib must be present, the python package is just a ctypes binding).
python-pam>=2.0
six>=1.16  # transitive dep of python-pam, listed explicitly so a fresh
           # install pulls it (the wheel doesn't always declare it).

# Optional: подцепим, когда переключим брокер с InMemoryBroker на Redis
# или подключим Socket.IO. Файлы services/redis/, web/sockets/ ссылаются
# на эти пакеты, но в основном пути они не импортируются.
redis>=5.0
python-socketio>=5.11

# dev / test
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21
